{{/*
  Migration hook — no longer needed.
  All agents have been upgraded to cluster-admin binding (chart 0.10.0+).
  This file is kept empty to avoid Helm "missing template" errors on
  downgrades. Safe to delete in a future chart version.
*/}}
